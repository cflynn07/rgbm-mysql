name: Docker Image CI
on:
  push:
    paths:
      - '*' # delete later
      - 'rgbm.sql'
      - 'Dockerfile-mysql'
    branches:
      - master
jobs:
  build_and_push:
    name: build and push
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: ls -l
      - name: Build
        run: docker build . --file Dockerfile-mysql --tag rgbm-mysql:$(date +%s)
